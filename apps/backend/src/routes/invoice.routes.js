const express = require('express');

const router = express.Router();

// Placeholder for future invoice financing endpoints
router.get('/', (req, res) => {
  res.json({ success: true, message: 'Invoice APIs not implemented yet' });
});

module.exports = router;

